import Coding from '../components/git/variable-length/coding/Coding.jsx';
import Decoding from '../components/git/variable-length/decoding/Decoding.jsx'

export const description =
  ''

# 变长编码

## 编码
```
这里有个问题我还是没有找到解决办法，就是依次读入显示这个问题，CSS和JS单独写是正确的，但是我没有找到一个合适的方法去解
决放在这里无法找到对应元素的问题（会报原型为空），可能是react这里因为是随机个数的表格导致无法找到对应组件（但是打印出
来不为空）也可能是格式冲突，这个问题还待解决
```
<div>
    <Coding/>
</div>

## 解码

<div>
    <Decoding/>
</div>
